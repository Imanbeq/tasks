/*
const giveNumber = (x) => {
    let inputNum = prompt('Введите число');
    while (inputNum != 99) {
        x++;
        inputNum = prompt('Введите число');
    }
    return ('Количество чисел - ') +  x;
}

const sumNumbers = (y, sum) => {
    let inputNum1 = parseFloat(prompt('Введите число'));
    while (inputNum1 != 999) {
        y++;
        sum += inputNum1;
        inputNum1 = parseFloat(prompt('Введите число'));
    }
    return ('Количество чисел - ') + y + (' сумма - ') + sum;

}

const showResult = (text) => {
    console.log(text);
}
showResult(giveNumber(0));
showResult(sumNumbers(0, 0));
*/

/*
const showHowOld = () => {
    let age = parseFloat(prompt('Введите ваш год рождения'));
    let year = parseFloat(prompt('Введите нынешний год'));
    return `Вам ${age - year} лет`;
}
alert(showHowOld());
*/

/*
const showMessage = (text) => {
    let x = 0;
    const intervalId = setInterval(() => {
        console.log(text);
        x++;
        if(x === 5){
            clearInterval(intervalId);
            console.log('Done');
        }
    }, 1000)
}
showMessage('Hello world');
*/



















